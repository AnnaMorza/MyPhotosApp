<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Base.Theme.MyPhotos" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Переопределяем основной цвет (влияет на статус-бар) -->
        <item name="colorPrimary">#000000</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Явно задаём цвет статус-бара -->
        <item name="android:statusBarColor">#000000</item>
        <item name="android:navigationBarColor">#000000</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <style name="Theme.MyPhotos" parent="Base.Theme.MyPhotos" />
</resources>